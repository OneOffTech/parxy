name: CI

on: 
  push:
    branches: 
      - "main"
  pull_request:
    types: [opened, synchronize, reopened]


concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    strategy:
      fail-fast: true
      matrix:
        python: [3.12, 3.13]
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Install the latest version of uv and set the python version
      uses: astral-sh/setup-uv@v6.7.0
      with:
        enable-cache: true
        python-version: ${{ matrix.python }}
    
    - name: Pull dependencies
      run: uv sync --all-extras --all-groups
    
    - name: Execute tests
      run: |
        uv run pytest -qs ./tests/drivers/test_pymupdf.py
        uv run pytest -qs ./tests/drivers/test_unstructured_local.py
        uv run pytest -qs ./tests/test_parxy_facade.py
        uv run pytest -qs ./tests/test_models.py
        uv run pytest -qs ./tests/test_base.py
